/**
 * @prettier
 */
import React from 'react';
import classes from 'src/styles/linearProgress.module.css';

export function LinearProgress(): React.ReactElement {
    return <progress className={classes.lineaProgress} />;
}
