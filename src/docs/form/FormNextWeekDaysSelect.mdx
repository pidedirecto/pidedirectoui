import { Meta, ArgTypes, Canvas} from '@storybook/blocks';

import * as FormNextWeekDaysSelectStories from '../../stories/form/FormNextWeekDaysSelect.stories'

<Meta title='form/FormNextWeekDaysSelect' />

# FormNextWeekDaysSelect
FormNextWeekDaysSelect component, renders a select with the next 7 days as options.

<Canvas of={FormNextWeekDaysSelectStories.Primary} sourceState={'none'} />

## API

<ArgTypes of={FormNextWeekDaysSelectStories} />

