import { ArgTypes, Canvas, Meta } from '@storybook/blocks';

import * as TextAreaStories from '../../stories/TextArea.stories';

<Meta of={TextAreaStories} />

# TextArea

Renders a textarea

<Canvas of={TextAreaStories.Primary} />

`TextArea` works in general the same as `Input` excepting this component does not have types.

## API

Here there are described some of the important props, but the `TextArea` component can receive almost any of the HTML textarea props.

<ArgTypes of={TextAreaStories} />
