import { ArgTypes, Canvas, Meta } from '@storybook/blocks';

import * as SkeletonLoaderStories from '../../stories/SkeletonLoader.stories';

<Meta of={SkeletonLoaderStories} />

# SkeletonLoader

Renders a skeleton loader

## Variants

-   notification

<Canvas of={SkeletonLoaderStories.Notification} />

## API

<ArgTypes of={SkeletonLoaderStories} />
